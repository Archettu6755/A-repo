# AGENTS.md —— 开发代理工作规则

本文件用于指导 AI 开发代理（Agent）在本项目中工作。所有开发行为必须遵守以下规则。

## 1. 需求约束

1. `target.md` 中“不要猜测我的意图，任何不明确的地方都应该向我提问”是最高优先级的长期约束。文件中的其他内容是旧约定，可由用户后来确认的新要求覆盖。
2. **严格遵守 `docs/proposal.md` 中的当前需求文档**，不得擅自更改、添加或删除功能需求。
3. 若实现过程中发现需求不明确、存在矛盾，或需要新增/修改功能，**必须向用户提问确认**，不得猜测用户意图。
4. 需求文档中的"默认数值"发生变更时，必须同步更新需求文档。
5. 任何新增玩法、数值调整、界面改动，都应先确认是否需要同步更新 `docs/proposal.md`。

## 2. Git 版本管理规则

### 2.1 基本约定

- 项目根目录为 `game/`，git 仓库也在此目录下。
- 单人开发，所有开发直接在 `main` 分支上进行（不强制使用 feature 分支）。
- 每次提交前先 `git status` 确认没有误加文件（缓存、密钥等）。
- 仓库尚未初始化时先执行 `git init`。
- 每日开发结束前必须提交一次，防止丢失。

### 2.2 提交时机

- **每完成一个功能**立即提交。
- **每修复一个 bug**立即提交。
- 不要积攒大量改动后才提交。

### 2.3 提交信息格式

使用中文，格式为 `类型: 说明`：

| 类型 | 用途 | 示例 |
| ---- | ---- | ---- |
| feat | 新功能 | `feat: 实现士兵移动` |
| fix | 修复 bug | `fix: 修复子弹穿墙问题` |
| docs | 文档修改 | `docs: 更新需求文档` |
| style | 样式/格式调整 | `style: 调整代码缩进` |
| refactor | 重构（不改功能） | `refactor: 拆分商店代码` |

提交命令：

```bash
git add .
git commit -m "feat: 实现士兵移动"
```

### 2.4 提交要求

- 提交信息必须写清楚"做了什么"，禁止模糊信息（如 `update`、`修改`）。
- 代码能运行、能启动才提交；提交"半成品"必须在信息中说明当前状态。
- **只有用户明确要求时才执行提交/推送**，否则不主动提交。

### 2.5 忽略文件规则（.gitignore）

项目根目录 `.gitignore` 内容如下，不得提交其中的文件：

```text
# Python 缓存
__pycache__/
*.pyc
*.pyo

# 虚拟环境
.venv/
venv/

# 运行日志与系统文件
*.log
.DS_Store
Thumbs.db
```

### 2.6 常用命令速查

| 命令 | 作用 |
| ---- | ---- |
| `git init` | 初始化仓库（仅第一次） |
| `git status` | 查看当前改动 |
| `git add .` | 添加全部改动 |
| `git commit -m "说明"` | 提交 |
| `git log --oneline` | 查看提交历史 |
| `git diff` | 查看未提交的具体改动 |

### 2.7 回退方法（代码出问题时）

```bash
git log --oneline          # 查看提交历史
git restore .              # 放弃未提交的改动
git reset --hard HEAD~1    # 回退到上一个版本（慎用，会丢弃改动）
```

## 3. 环境与运行

- Python 环境使用 **uv** 管理（虚拟环境位于 `.venv/`）。
- 安装依赖：`uv add 包名`；运行程序：`uv run game`。
- 不要手动修改 `uv.lock` 和 `pyproject.toml` 的依赖部分，统一通过 `uv add/remove` 管理。
- 运行命令、验证操作均从 `game/` 目录执行。

## 4. 代码风格与规范检查

- 遵循 `docs/proposal.md`、`docs/map.md` 和 `docs/art.md` 中确定的玩法、地图和美术约定。
- 代码结构保持地图数据、碰撞数据、动画状态与绘制分离。正式贴图不得直接决定碰撞框，角色按脚底锚点定位。

### 4.1 Ruff 代码规范检查

项目使用 **Ruff** 进行代码 lint 与格式化，命令如下：

```bash
uv run ruff check .     # 检查代码规范问题
uv run ruff format .    # 自动格式化代码
```

规则：

1. **每次完成代码修改后**，必须运行 `uv run ruff check .`，确保无警告/错误。
2. **提交前**必须通过 Ruff 检查，禁止提交有 lint 错误的代码。
3. Ruff 检查发现问题时，优先通过 `uv run ruff format .` 自动修复；无法自动修复的按 Ruff 提示人工修改。
4. 新增依赖、修改配置时不得破坏现有代码的规范状态。
